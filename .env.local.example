
# Firebase Client SDK Configuration
# =================================
# Copy this file to .env.local and replace the placeholder values
# with your actual Firebase project credentials.
# You can find these in your Firebase project settings:
# Project settings > General > Your apps > Web app > SDK setup and configuration > Config
#
# IMPORTANT: Only variables prefixed with NEXT_PUBLIC_ will be exposed to the browser.
# DO NOT put sensitive server-side keys (like Firebase Admin SDK private key) here.

NEXT_PUBLIC_FIREBASE_API_KEY=YOUR_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=YOUR_PROJECT_ID.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=YOUR_PROJECT_ID
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=YOUR_PROJECT_ID.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=YOUR_MESSAGING_SENDER_ID
NEXT_PUBLIC_FIREBASE_APP_ID=YOUR_APP_ID
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=YOUR_MEASUREMENT_ID # Optional, for Google Analytics

# Firebase Admin SDK Configuration (Server-Side Only)
# =================================
# These are used in src/lib/firebase/firebase-admin.ts and should NOT start with NEXT_PUBLIC_
# Store these securely in your deployment environment variables.
# For local development, you can add them to .env.local, but be careful not to commit this file.
#
# Go to Project settings > Service accounts > Generate new private key
# This will download a JSON file. Extract the required values:

# FIREBASE_PROJECT_ID=YOUR_PROJECT_ID_FROM_SERVICE_ACCOUNT_JSON
# FIREBASE_CLIENT_EMAIL=YOUR_CLIENT_EMAIL_FROM_SERVICE_ACCOUNT_JSON
# FIREBASE_PRIVATE_KEY="YOUR_PRIVATE_KEY_FROM_SERVICE_ACCOUNT_JSON_WITH_\n_FOR_NEWLINES"

# Example Admin User Credentials (Server-Side Only)
# =================================
# Used in src/app/admin/login/actions.ts
# Store these securely in your deployment environment variables.

# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=Admin123@ # Change this in production!

# Google AI API Key (Server-Side Only)
# =================================
# Used for Genkit features (e.g., image generation, LLM calls).
# Obtain from Google AI Studio: https://aistudio.google.com/app/apikey
# Store securely in your deployment environment variables.

# GOOGLE_GENAI_API_KEY=YOUR_GOOGLE_AI_API_KEY

# SMS Provider API Keys (Server-Side Only)
# =================================
# Example for Kavenegar (replace with your actual provider if different)
# Store securely in your deployment environment variables.

# KAVENEGAR_API_KEY=YOUR_KAVENEGAR_API_KEY # Example

```