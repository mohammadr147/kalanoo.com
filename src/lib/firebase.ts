
// This file is intentionally left blank as Firebase client SDK is not used.
// Configuration and initialization logic have been removed.

// Ensure necessary exports are null or undefined if other files still import them temporarily.
export const app = null;
export const auth = null;
export const db = null;
export const functions = null;
